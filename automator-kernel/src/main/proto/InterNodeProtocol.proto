syntax = "proto2";
package com.bbles.automator.node.protobuf;
// Registration Protocol
message Ack {
    required string secret = 1;
    required string name = 2;
}

service AckService {
    rpc ackNode (Ack) returns (Ack);
}